# Gitignored-файлы, которые нужно переносить в новые git worktree,
# чтобы приложение и тесты поднимались (config читает их на импорте).
# По одному пути на строку, относительно корня репозитория.
.env
firebase-adminsdk.json
wishes-product
plans
